runs:   
  using: "composite"
  steps:
    - name: ... build success ...
      run: echo Building of node application is starting ..
      shell: cmd

    - name: Install dependencies
      run: npm install
      shell: cmd